apiVersion: v1
kind: Service
metadata:
  name: {{ include "family-frame.fullname" . }}-frontend
  labels:
    {{- include "family-frame.componentLabels" (dict "root" . "component" "frontend") | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  selector:
    {{- include "family-frame.selectorLabels" (dict "root" . "component" "frontend") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
