apiVersion: v1
kind: Service
metadata:
  name: {{ include "family-frame.fullname" . }}-backend
  labels:
    {{- include "family-frame.componentLabels" (dict "root" . "component" "backend") | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  selector:
    {{- include "family-frame.selectorLabels" (dict "root" . "component" "backend") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
