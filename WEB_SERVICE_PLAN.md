# Family Frame 웹 서비스 기획서

> 문서 상태: 구현 전 기준안  
> 작성 기준일: 2026-07-31  
> 프로젝트 기간: 2026-07-15 ~ 2026-08-18  
> 작업명: 쑥쑥찰칵 사례 기반 이미지 스토리지 FinOps 비용 절감 솔루션

## 1. 문서 목적

이 문서는 `nn.zip`에 포함된 Notion 내보내기 자료와 현재 `family-frame` 저장소를 바탕으로 웹 서비스의 제품 범위, 사용자 경험, 화면 구조, 데이터와 API, 시스템 연동, 품질 기준 및 구현 순서를 하나의 기준으로 정리한다.

웹 서비스는 전체 시스템 위에서 동작하는 실제 사용자용 애플리케이션이다. 이 문서의 책임 범위는 가족 앨범의 Frontend/Backend와 PostgreSQL·Ceph 연동까지다. Kubernetes 운영, 모니터링, Grafana, 장애 대응 및 FinOps 대시보드는 다른 담당 영역이며 웹 구현 범위에 포함하지 않는다.

## 2. 확인한 자료와 현재 상태

### 2.1 입력 자료

`nn.zip`은 Notion Export ZIP을 한 번 더 감싼 이중 압축 구조다. 내부 자료는 총 70개다.

- Markdown 39개
- DOCX 회의록 6개
- PNG 21개
- JPG 1개
- HTML/CSS/JavaScript 프로토타입 각 1개

주요 내용은 다음과 같다.

- 경쟁 서비스와 비용 문제 분석
- Kubernetes 및 Rook-Ceph 기반 인프라 구성
- RadosGW, PostgreSQL, Erasure Coding 설계
- GitLab CI/CD, SonarQube, Harbor, Helm, Terraform 구성
- 팀 역할, 주차별 계획, 데일리 스크럼 및 회의록
- 모바일 캘린더 형태의 아기 성장 앨범 프로토타입
- 웹 범위 밖의 인프라·운영·대시보드 자료

GitHub 사용법 관련 스크린샷은 개발 절차 참고 자료이며 제품 기능 기획에는 직접 반영하지 않았다.

### 2.2 현재 저장소

현재 저장소에는 `AGENTS.md`, 설치된 agent skill 및 lock 파일만 있고 실제 애플리케이션 코드는 없다. 문서에서 예정한 `frontend/`와 `backend/` 디렉터리도 아직 생성되지 않았다.

따라서 이 기획은 기존 서비스의 개선안이 아니라 신규 구축 기준이다.

## 3. 프로젝트에 대한 이해

### 3.1 해결하려는 문제

사진 중심 서비스의 비용은 단순 저장 용량만으로 결정되지 않는다.

- 같은 사진의 반복 업로드
- 원본 이미지를 목록에서도 그대로 전송하는 비효율
- 매일 반복되는 목록 조회와 객체 요청
- 이미지 파생본과 메타데이터의 관리 비용
- 장애 시 복구와 데이터 정합성 유지 비용
- 트래픽과 비용의 관계를 확인하기 어려운 운영 구조

전체 프로젝트는 아기 성장 앨범을 실제 워크로드로 삼는다. 웹 서비스는 그중 다음 애플리케이션 흐름을 책임진다.

1. 사진 원본과 메타데이터를 분리한다.
2. 원본과 썸네일은 Ceph RadosGW에 비공개 객체로 저장한다.
3. 계정, 가족, 앨범, 사진 상태 및 객체 키는 PostgreSQL에 저장한다.
4. 중복 제거와 썸네일 우선 전송으로 저장량과 전송량을 줄인다.
5. 저장소 장애나 부분 실패가 발생해도 사용자에게 잘못된 완료 상태를 보여주지 않는다.

### 3.2 웹 서비스의 역할

웹은 다음 두 역할을 담당한다.

- 사용자 가치: 부모와 조부모가 아이의 사진을 날짜별로 쉽게 기록하고 함께 본다.
- 애플리케이션 완성도: 인증, 권한, 업로드, 이미지 처리 및 조회를 하나의 안정적인 서비스로 제공한다.

### 3.3 한 문장 제품 정의

**Family Frame은 가족만 초대해 아이의 성장 사진을 날짜별로 모으고, 가볍고 안전하게 다시 보는 비공개 가족 앨범이다.**

## 4. 제품 원칙

1. **가족 화면에는 가족의 언어만 사용한다.**  
   Ceph, pHash, OSD 같은 시스템 용어를 사용자 화면에 노출하지 않는다.

2. **날짜가 탐색의 중심이다.**  
   사용자는 파일명이나 저장 위치가 아니라 “그날의 아이”를 찾는다.

3. **사진을 먼저 보여준다.**  
   캘린더는 대표 썸네일을, 상세 화면은 작은 이미지부터 보여준 뒤 필요한 원본만 불러온다.

4. **사적인 사진은 기본적으로 비공개다.**  
   공개 URL을 만들지 않으며 모든 사진 접근 전에 가족 권한을 다시 확인한다.

5. **웹 서비스 밖의 기능을 끌어오지 않는다.**  
   인프라 운영, 모니터링, 비용 분석 및 장애 대응 화면은 다른 담당 영역으로 둔다.

6. **시스템이 실패해도 상태를 숨기지 않는다.**  
   업로드 실패, 처리 중단, 객체 저장과 DB 기록의 불일치를 명확히 표시하고 재시도할 수 있게 한다.

## 5. 목표 사용자

### 5.1 주 사용자: 부모

- 아이 사진을 휴대폰에서 빠르게 올리고 싶다.
- 특정 날짜의 사진을 나중에 쉽게 찾고 싶다.
- 가족에게 매번 메신저로 사진을 다시 보내고 싶지 않다.
- 누가 사진을 볼 수 있는지 통제하고 싶다.

### 5.2 공동 사용자: 조부모와 가까운 가족

- 복잡한 메뉴 없이 최근 사진을 보고 싶다.
- 초대 링크로 간단히 가족 앨범에 들어오고 싶다.
- 필요하면 본인이 촬영한 사진도 올리고 싶다.
- 작은 글씨, 숨겨진 제스처, 불명확한 아이콘을 피해야 한다.

## 6. 범위

### 6.1 이번 프로젝트에서 완성할 범위

- 이메일과 비밀번호 기반 회원가입·로그인·로그아웃
- 최초 가족 및 아기 앨범 생성
- 가족 초대 링크 발급 및 참여
- 월간 사진 캘린더
- 날짜별 사진 목록
- 사진 업로드, 진행률, 실패 재시도
- 날짜당 최대 10장 제한
- 대표 사진 선택
- 전체 모아보기
- 사진 상세 보기와 삭제
- 원본·썸네일 분리 저장
- 비공개 객체와 단기 서명 URL
- 정확히 같은 파일의 중복 저장 방지
- 업로드 및 처리 상태 추적
- 최소 가족 설정과 멤버 관리
- 모바일·태블릿·데스크톱 반응형 UI
- 접근성, 오류, 빈 상태, 로딩 상태

### 6.2 이번 범위에서 제외

- 수유텀 기록
- 일기 및 육아일지
- 댓글, 좋아요, 이모지 반응
- 영상 업로드 및 재생
- 인화 상품과 결제
- 공개 앨범과 검색 엔진 노출
- AI 자동 분류, 얼굴 인식, 자동 캡션
- 유사 사진 자동 삭제
- 별도 모바일 앱
- Grafana·Prometheus·Loki 등 운영/모니터링 화면
- FinOps 대시보드와 비용 계산
- OSD 장애 주입 및 인프라 복구 자동화
- 마이크로서비스, 메시지 큐, 별도 워커

자료의 `수유텀`과 `일기` 탭은 참고 서비스의 UI 흔적으로 보이며 이번 웹 서비스의 핵심인 이미지 저장·조회와 직접 관련이 없다. 먼저 사진 앨범을 완성한 뒤 실제 수요가 확인될 때 추가한다.

## 7. 정보 구조

```text
공개 영역
├─ 서비스 소개 /
├─ 로그인 /login
├─ 회원가입 /signup
└─ 가족 초대 수락 /invite/:token

가족 앨범
├─ 시작 설정 /onboarding
├─ 월간 캘린더 /album
├─ 날짜별 사진 /album/:date
├─ 전체 모아보기 /gallery
├─ 사진 상세 /photos/:photoId
├─ 업로드 /upload?date=YYYY-MM-DD
└─ 가족 설정 /settings/family
```

하단 주 내비게이션은 `캘린더`, `모아보기`, `가족` 세 개만 둔다. 업로드는 캘린더와 날짜 상세 화면의 명확한 `사진 올리기` 버튼으로 진입한다.

## 8. 핵심 사용자 흐름

### 8.1 첫 가족 만들기

1. 회원가입한다.
2. 가족 앨범 이름과 아이 이름을 입력한다.
3. 아이 생일은 선택 입력으로 둔다.
4. 빈 캘린더와 `첫 사진 올리기` 안내를 본다.
5. 첫 사진을 올리면 해당 날짜가 대표 썸네일로 채워진다.

### 8.2 가족 초대

1. 소유자가 가족 설정에서 초대 링크를 만든다.
2. 초대받은 사람은 링크를 열고 가입 또는 로그인한다.
3. 초대 대상 가족 이름을 확인한 뒤 참여한다.
4. 참여 후 동일한 앨범을 조회하고 사진을 올릴 수 있다.
5. 만료되거나 이미 사용한 링크는 원인과 다음 행동을 알려준다.

### 8.3 사진 올리기

1. 캘린더에서 날짜를 선택하거나 `사진 올리기`를 누른다.
2. 날짜와 현재 등록 장수를 확인한다.
3. 사진을 최대 남은 수만큼 선택한다.
4. 파일 형식과 크기를 즉시 검사한다.
5. 각 사진의 업로드 진행률을 보여준다.
6. 서버가 파일을 검증하고 썸네일과 메타데이터를 만든다.
7. 준비된 사진부터 화면에 표시한다.
8. 같은 파일이 이미 있으면 중복 저장 없이 기존 객체를 재사용하고 사용자에게는 “같은 사진이라 저장 공간을 절약했어요”라고 안내한다.

### 8.4 사진 다시 보기

1. 월간 캘린더에서 대표 사진이 있는 날짜를 찾는다.
2. 날짜를 열어 썸네일 목록을 먼저 본다.
3. 사진을 선택하면 화면 크기에 맞는 이미지를 불러온다.
4. 이전·다음 사진으로 이동한다.
5. 권한이 있는 사용자는 대표 사진 지정 또는 삭제를 수행한다.

## 9. 화면별 기획

### 9.1 서비스 소개

목적은 제품 판매가 아니라 사용자가 서비스 성격과 비공개 원칙을 이해하고 로그인하게 하는 것이다.

- 실제 앨범 캘린더를 연상시키는 한 장의 대표 화면
- “가족의 하루를 날짜별로 모아요”라는 짧은 설명
- `로그인`, `가족 앨범 만들기` 두 행동
- 비공개 가족 전용, 원본 보호, 초대된 가족만 접근 가능하다는 신뢰 정보
- 인프라 기술 설명은 사용자용 웹 화면에 노출하지 않음

### 9.2 로그인·회원가입

- 이메일, 비밀번호
- 비밀번호 표시 전환
- 입력 중 오류와 서버 오류를 구분
- Enter 제출과 키보드 탐색 지원
- 로그인 후 원래 열려던 초대 또는 앨범 위치로 복귀

비밀번호 재설정 메일 연동은 외부 메일 시스템이 준비되지 않았다면 이번 데모 범위에서는 제외하고 명시적으로 안내한다.

### 9.3 시작 설정

- 가족 앨범 이름
- 아이 이름
- 생일 선택 입력
- 대표 색상이나 복잡한 테마 설정은 두지 않는다.
- 완료 후 캘린더로 바로 이동

### 9.4 월간 캘린더

이 서비스의 대표 화면이다.

- 상단: 가족/아이 이름, 알림 없는 단순 헤더
- 월 이동: 이전 달, `YYYY년 M월`, 다음 달
- 요일과 날짜 그리드
- 사진이 있는 날짜: 대표 썸네일과 항상 읽을 수 있는 날짜 숫자
- 사진이 없는 날짜: 조용한 빈 셀
- 오늘: 테두리로 구분
- 선택 날짜: 명확한 포커스와 하단 요약
- 하단 고정 행동: `사진 올리기`
- 월별 사진 수를 작은 보조 정보로 제공

자료 간 달력 월이 2022년 9월, 8월로 서로 다르므로 제품은 일반 월 이동을 지원하고 발표용 시드 데이터만 2026년 8월로 통일한다.

### 9.5 날짜별 사진

- 날짜, 등록 장수, 남은 업로드 수
- 대표 사진 우선 표시
- 썸네일 그리드
- `사진 올리기`, `대표 사진 선택`
- 사진이 없으면 해당 날짜의 첫 사진 업로드 유도
- 10장에 도달하면 이유와 삭제 방법을 함께 안내
- 처리 중 사진은 빈 이미지가 아니라 상태 카드로 표시

### 9.6 업로드

- 드래그앤드롭과 파일 선택을 모두 지원
- 모바일에서는 사진 라이브러리 선택 우선
- 지원 형식: JPEG, PNG, WebP
- 파일당 최대 크기: 초기 20MB
- 선택한 파일별 미리보기, 크기, 진행 상태
- 전체 취소보다 파일별 제거를 우선 제공
- 10장 제한은 클라이언트와 서버에서 모두 검사
- 업로드 완료와 사진 처리 완료를 구분
- 일부 실패 시 성공한 사진은 유지하고 실패한 항목만 재시도

### 9.7 모아보기

- 최신 촬영일순 썸네일 그리드
- 월 구분 헤더
- 초기에는 검색과 복잡한 필터를 두지 않는다.
- 더 보기 또는 커서 기반 무한 로딩
- 썸네일 우선, 화면 진입 전 원본 선로딩 금지

### 9.8 사진 상세

- 화면에 맞는 파생 이미지
- 촬영 날짜와 업로더
- 이전·다음 탐색
- 대표 사진 지정
- 삭제 전 명확한 확인
- 키보드, 스와이프 외 버튼 탐색 모두 지원
- 원본 다운로드는 명시적 행동으로만 제공

### 9.9 가족 설정

- 가족 앨범 이름과 아이 정보
- 현재 멤버 목록
- 초대 링크 생성, 복사, 만료
- 소유자만 멤버 제거 및 가족 설정 변경
- 일반 멤버도 사진 조회와 업로드 가능

초기 권한은 `OWNER`, `MEMBER` 두 개만 사용한다. 별도 조회 전용 역할은 요구가 확인될 때 추가한다.

## 10. 디자인 방향

### 10.1 디자인 명제

**“날짜가 곧 사진 프레임이 되는 디지털 성장 앨범”**

자료의 원형 날짜 썸네일을 그대로 복제하기보다 날짜 셀 전체가 작은 사진 프레임으로 변하는 방식을 사용한다. 이것이 서비스의 한 가지 대표 시각 요소다. 나머지 화면은 사진을 방해하지 않도록 절제한다.

### 10.2 색상 토큰

- Album paper `#F7F8FC`: 기본 배경
- Family ink `#18233D`: 제목과 주요 텍스트
- Frame blue `#4F6FE8`: 선택, 링크, 주요 행동
- Growth yellow `#FFD45A`: 오늘과 따뜻한 강조
- Sprout mint `#58C7A5`: 완료와 성공 상태
- Clear coral `#D95757`: 오류와 위험 행동

사진의 색이 주인공이므로 배경 그라데이션과 장식용 색면은 최소화한다.

### 10.3 타이포그래피

- 앨범 제목과 월: `Gowun Batang`, 제한적으로 사용
- 본문과 컨트롤: `Pretendard`
- 운영 수치와 상태: `IBM Plex Mono`

한글 본문은 모바일에서 최소 16px를 유지한다. 조부모 사용성을 위해 보조 텍스트도 14px 아래로 내리지 않는다.

### 10.4 레이아웃

모바일:

```text
┌──────────────────────┐
│ Family Frame   가족  │
│ 하린이의 앨범        │
├──────────────────────┤
│ ‹   2026년 8월   ›   │
│ 일 월 화 수 목 금 토 │
│ □  □  ▣  □  ▣  □  □ │
│ ▣  ▣  □  □  ▣  □  □ │
│ ...                  │
├──────────────────────┤
│ 8월 12일 · 사진 4장  │
│ [사진 올리기]        │
├──────────────────────┤
│ 캘린더  모아보기 가족│
└──────────────────────┘
```

데스크톱:

```text
┌──────────────┬─────────────────────────────┐
│ 앨범 정보    │ 월간 캘린더                 │
│ 최근 날짜    │                             │
│ 가족 멤버    │ 선택 날짜 사진 미리보기     │
│              │                       업로드 │
└──────────────┴─────────────────────────────┘
```

데스크톱은 모바일 화면을 가운데에 단순 확대하지 않고, 캘린더와 선택 날짜 미리보기를 나란히 사용한다.

### 10.5 모션

- 월 이동 시 방향을 알 수 있는 짧은 전환 한 번
- 업로드 완료 시 해당 날짜가 사진 프레임으로 채워지는 전환
- 나머지 요소에는 반복적인 부유·반짝임 애니메이션을 사용하지 않는다.
- `prefers-reduced-motion`에서 모든 비필수 전환을 제거한다.

### 10.6 문구 원칙

- 시스템 구조가 아니라 사용자가 하는 일을 말한다.
- 버튼은 결과가 명확한 동사를 쓴다.
- 같은 행동은 시작부터 완료 메시지까지 같은 이름을 유지한다.

예:

- `Submit` → `사진 올리기`
- `Object processing failed` → `사진을 준비하지 못했습니다. 다시 시도해 주세요.`
- 빈 캘린더 → `아직 사진이 없어요. 첫 성장 사진을 올려보세요.`
- 10장 초과 → `이 날짜에는 사진을 10장까지 올릴 수 있어요. 기존 사진을 지운 뒤 다시 시도해 주세요.`

## 11. 권한 정책

| 행동 | 비로그인 | MEMBER | OWNER |
|---|---:|---:|---:|
| 공개 소개 보기 | 가능 | 가능 | 가능 |
| 가족 앨범 보기 | 불가 | 소속 가족만 | 소속 가족만 |
| 사진 업로드 | 불가 | 가능 | 가능 |
| 본인 업로드 삭제 | 불가 | 가능 | 가능 |
| 다른 멤버 사진 삭제 | 불가 | 불가 | 가능 |
| 대표 사진 지정 | 불가 | 가능 | 가능 |
| 초대 생성·멤버 제거 | 불가 | 불가 | 가능 |

## 12. 도메인 모델

### 12.1 핵심 엔터티

`User`

- id
- email
- passwordHash
- createdAt

`Session`

- id
- userId
- tokenHash
- expiresAt
- createdAt

`Family`

- id
- name
- ownerId
- createdAt

`FamilyMember`

- familyId
- userId
- role: `OWNER | MEMBER`
- joinedAt

`FamilyInvite`

- id
- familyId
- tokenHash
- expiresAt
- acceptedAt
- createdBy

`Album`

- id
- familyId
- childName
- childBirthDate
- createdAt

`Photo`

- id
- albumId
- uploaderId
- albumDate
- capturedAt
- originalObjectKey
- thumbnailObjectKey
- contentHash
- perceptualHash
- mimeType
- originalBytes
- storedBytes
- width
- height
- status: `PENDING_UPLOAD | PROCESSING | READY | FAILED | DELETED`
- failureReason
- createdAt
- deletedAt

`DailyRepresentative`

- albumId
- albumDate
- photoId

### 12.2 핵심 제약

- 가족 이메일은 서비스 전체에서 유일하다.
- 한 사용자는 소속 가족의 데이터만 읽고 쓸 수 있다.
- 한 앨범과 날짜 조합에는 `READY` 또는 처리 중인 사진이 최대 10개다.
- 대표 사진은 같은 앨범과 같은 날짜에 속한 유효한 사진이어야 한다.
- 필수 파생본과 메타데이터가 준비되기 전에는 `READY`가 될 수 없다.
- 같은 완료 요청을 반복해도 사진이나 객체가 중복 생성되지 않아야 한다.
- 공개 객체 URL을 DB에 영구 저장하지 않는다.

## 13. 인증과 보안

- 비밀번호는 Node.js 표준 `crypto.scrypt`로 해시한다.
- 로그인 세션은 충분히 긴 난수 토큰을 발급하고 DB에는 토큰 해시만 저장한다.
- 세션 쿠키는 `HttpOnly`, `Secure`, `SameSite=Lax`를 사용한다.
- 상태 변경 요청은 Origin 검사와 CSRF 방어를 적용한다.
- 로그인과 초대 수락에는 요청 속도 제한을 적용한다.
- 모든 API는 클라이언트가 보낸 `userId`, `familyId`, role 또는 객체 키만 믿지 않고 세션과 DB 관계로 권한을 계산한다.
- RGW Access/Secret Key는 백엔드 환경 변수에만 존재한다.
- 버킷은 비공개로 두고 짧은 만료 시간을 가진 서명 URL만 발급한다.
- 업로드 시 확장자가 아니라 magic byte, MIME, 파일 크기 및 이미지 디코딩 성공 여부를 검사한다.
- 사용자에게 제공하는 파생 이미지에서는 GPS EXIF를 제거한다.
- 감사 대상 행동인 로그인 실패, 초대 생성, 멤버 제거, 사진 삭제는 구조화 로그로 남긴다.

## 14. 이미지 저장 및 처리

### 14.1 객체 구조

```text
baby-album-store/
├─ originals/{familyId}/{albumId}/{photoId}/original
└─ derivatives/{familyId}/{albumId}/{photoId}/thumb.webp
```

객체 키에는 사용자가 올린 파일명을 직접 사용하지 않는다.

### 14.2 업로드 처리

1. 클라이언트가 날짜, 파일명, 크기 및 MIME으로 업로드 시작을 요청한다.
2. NestJS가 세션, 가족 권한, 날짜당 장수 및 파일 조건을 검사한다.
3. `PENDING_UPLOAD` 레코드를 만든다.
4. 짧게 유효한 RGW presigned PUT URL을 반환한다.
5. 브라우저가 RGW로 파일을 업로드한다.
6. 클라이언트가 완료 API를 호출한다.
7. 백엔드가 객체 존재와 크기를 확인한다.
8. 객체를 읽어 SHA-256, 이미지 크기, 촬영 시각을 구하고 Sharp로 WebP 썸네일을 만든다.
9. 동일 SHA-256 객체가 있으면 기존 원본을 재사용하고 방금 올라간 중복 객체를 제거한다.
10. DB 트랜잭션으로 객체 키, 메타데이터, 파생본 및 상태를 `READY`로 확정한다.

별도 워커 없이 완료 요청에서 처리한다. 사진 크기와 처리 시간이 실제로 문제가 될 때만 큐와 워커를 도입한다.

### 14.3 중복 처리 원칙

- SHA-256이 같은 파일은 안전하게 자동 중복 제거한다.
- perceptual hash는 유사 사진 후보를 찾는 데만 사용한다.
- 유사하다는 이유만으로 사용자 사진을 자동 삭제하지 않는다.
- 유사 사진 정리는 후속 기능에서 사용자 확인을 받은 뒤 제공한다.

### 14.4 조회

- 캘린더와 모아보기는 썸네일 URL만 요청한다.
- 상세 화면도 화면 크기에 맞는 파생본을 우선 사용한다.
- 원본은 사용자가 `원본 보기` 또는 `다운로드`를 선택할 때만 요청한다.
- 서명 URL 발급 전에 매번 가족 소속과 사진 상태를 확인한다.

## 15. API 초안

### 15.1 인증

```text
POST   /api/auth/signup
POST   /api/auth/login
POST   /api/auth/logout
GET    /api/auth/me
```

### 15.2 가족과 초대

```text
POST   /api/families
GET    /api/families/current
PATCH  /api/families/current
GET    /api/families/current/members
DELETE /api/families/current/members/:memberId
POST   /api/families/current/invites
POST   /api/invites/:token/accept
```

### 15.3 앨범과 사진

```text
GET    /api/albums/current
GET    /api/albums/current/calendar?month=YYYY-MM
GET    /api/albums/current/dates/:date/photos
GET    /api/albums/current/photos?cursor=...
POST   /api/albums/current/photos/uploads
POST   /api/photos/:photoId/complete
POST   /api/photos/:photoId/retry
GET    /api/photos/:photoId
GET    /api/photos/:photoId/url?variant=thumbnail|display|original
PUT    /api/albums/current/dates/:date/representative
DELETE /api/photos/:photoId
```

모든 요청과 응답 DTO는 Swagger/OpenAPI로 문서화한다. 목록은 offset보다 안정적인 cursor pagination을 사용한다.

## 16. 전체 시스템 흐름

```text
사용자 브라우저
  │
  ▼
Ingress NGINX
  ├─ /        → Next.js
  ├─ /api     → NestJS
  └─ 서명 URL → Ceph RadosGW
                 │
NestJS ──────────┼─ PostgreSQL: 계정·권한·메타데이터·상태·해시
                 └─ Ceph RGW: 원본·썸네일 비공개 객체
```

Next.js route handler에 NestJS와 중복되는 비즈니스 API를 만들지 않는다. NestJS가 인증, 권한, 사진 상태 및 저장 규칙의 유일한 기준이다.

## 17. 웹 서비스 책임 경계

웹 팀이 구현할 대상:

- Next.js 사용자 화면
- NestJS 비즈니스 API
- 인증과 가족별 접근 제어
- PostgreSQL 애플리케이션 스키마
- Ceph RadosGW 업로드·조회 연동
- 이미지 검증, 썸네일, 중복 방지 및 상태 관리
- 사용자에게 보이는 오류와 재시도

다른 담당 영역:

- Kubernetes 클러스터 구성과 운영
- Grafana, Prometheus, Loki, Fluentd 및 Alertmanager
- FinOps 비용 산정과 대시보드
- Ceph OSD 장애 주입과 복구 검증
- CI/CD, Harbor, Helm 및 Terraform

웹 팀은 배포에 필요한 환경 변수와 readiness 계약을 인프라 담당자와 합의하지만, 해당 운영 도구나 대시보드를 웹 기능으로 구현하지 않는다.

## 18. 성능 목표

- 모바일 기준 주요 화면 LCP 2.5초 이내를 목표로 한다.
- 캘린더 API는 한 달의 날짜, 장수, 대표 썸네일 정보만 반환한다.
- 썸네일은 기본 320px WebP로 생성하고 원본을 목록에 사용하지 않는다.
- 화면 아래 이미지는 `loading="lazy"` 또는 동등한 지연 로딩을 사용한다.
- 한 화면에서 필요한 서명 URL만 발급한다.
- 중복 데이터 요청은 Next.js 서버 렌더링 또는 클라이언트 캐시에서 합친다.
- 업로드는 파일별 진행률과 재시도를 제공한다.
- 이미지 처리 때문에 API가 느려지면 먼저 크기 제한과 파생본 수를 조정하고, 측정 결과가 필요성을 증명할 때만 워커를 추가한다.

## 19. 접근성과 반응형 기준

- WCAG 2.2 AA 수준을 목표로 한다.
- 모든 행동은 키보드로 수행할 수 있어야 한다.
- 포커스 표시를 제거하지 않는다.
- 터치 대상은 최소 44×44px로 한다.
- 날짜는 색상과 사진 유무만으로 상태를 전달하지 않는다.
- 대표 썸네일 위 날짜 숫자는 배경과 충분한 대비를 갖는다.
- 이미지에는 의미 있는 대체 텍스트 또는 날짜 기반 설명을 제공한다.
- 업로드 진행률은 시각 표시와 텍스트 상태를 함께 제공한다.
- 오류 발생 시 문제가 된 파일과 해결 방법을 연결한다.
- 모바일 360px부터 데스크톱까지 가로 스크롤 없이 동작한다.
- 브라우저 확대 200%에서도 핵심 작업이 가능해야 한다.

## 20. 오류와 복구

| 상황 | 사용자 처리 | 시스템 처리 |
|---|---|---|
| 지원하지 않는 파일 | 선택 단계에서 파일별 이유 표시 | 업로드 URL 미발급 |
| 10장 초과 | 남은 장수와 삭제 방법 안내 | 트랜잭션 안에서 다시 검사 |
| RGW 업로드 실패 | 해당 파일만 재시도 | 상태 유지 및 만료 처리 |
| 완료 호출 중단 | 처리 중 표시와 재시도 | idempotency key로 중복 방지 |
| 썸네일 생성 실패 | 원본을 노출하지 않고 실패 표시 | `FAILED`와 실패 코드 기록 |
| DB 기록 실패 | 완료되지 않은 상태 표시 | 고아 객체를 운영 목록에 노출 |
| 객체 누락 | 사진 준비 실패 안내 | reconciliation 대상으로 등록 |
| 세션 만료 | 로그인 후 원래 위치 복귀 | 보호 API 401 |
| 다른 가족 접근 | 일반적인 접근 불가 안내 | 403 및 감사 로그 |

고아 객체와 오래된 `PENDING_UPLOAD`를 찾는 reconciliation 기능을 먼저 제공한다. 자동 정리 작업은 실제 누적 문제가 확인된 뒤 추가한다.

## 21. 구현 단계

### Phase 0. 기준 확정 — 7/31

- 이 기획서 합의
- 데모 월과 시드 데이터 확정
- 외부 접속 도메인, TLS 및 RGW 서명 URL 경로 확인
- 팀별 API·인프라 연결 경계 확정

완료 조건:

- 화면, API, 데이터 모델 및 배포 경계에 팀 합의가 있다.

### Phase 1. 뼈대와 인증 — 8/1~8/3

- `frontend/` Next.js 프로젝트
- `backend/` NestJS 프로젝트
- PostgreSQL/Prisma 연결
- User, Session, Family, Member, Album 모델
- 회원가입, 로그인, 로그아웃, 보호 라우트
- 가족 생성과 시작 설정

완료 조건:

- 사용자가 가입하고 본인 가족의 빈 앨범에만 들어갈 수 있다.

### Phase 2. 핵심 앨범 — 8/4~8/7

- 월간 캘린더
- 날짜별 목록
- 업로드 시작과 RGW 저장
- Sharp 썸네일
- 사진 상태 관리
- 10장 제한과 대표 사진
- 모아보기와 상세 화면

완료 조건:

- 휴대폰에서 사진을 올리고 캘린더와 상세 화면에서 다시 볼 수 있다.

### Phase 3. 가족 공유와 저장 최적화 — 8/8~8/10

- 초대 링크와 멤버 관리
- SHA-256 중복 제거
- 서명 URL과 권한 검증
- 저장 절감 이벤트
- 실패 재시도와 reconciliation

완료 조건:

- 두 계정이 같은 가족 앨범을 공유하며 다른 가족 데이터에는 접근할 수 없다.
- 같은 파일을 반복 업로드해도 원본 객체가 중복 저장되지 않는다.

### Phase 4. 사용자 완성도 — 8/11~8/13

- 가족 설정과 멤버 관리
- 반응형 레이아웃
- 접근성
- 빈 상태, 로딩, 오류 및 재시도
- 이미지 성능

완료 조건:

- 부모와 조부모가 모바일에서 핵심 흐름을 막힘없이 사용할 수 있다.

### Phase 5. 완성도와 검증 — 8/14~8/16

- 통합 테스트와 브라우저 테스트
- PostgreSQL·RGW 부분 실패와 재시도 검증

완료 조건:

- 인증, 권한, 업로드, 조회 및 실패 복구가 통합 환경에서 동작한다.

### Phase 6. 발표 준비 — 8/17~8/18

- 재현 가능한 시드 데이터
- 데모 계정
- 데모 순서와 역할 분담
- README와 API 문서 정리

완료 조건:

- 신규 환경에서도 정해진 순서로 동일한 결과를 재현할 수 있다.

## 22. 테스트 전략

### 22.1 가장 중요한 자동 테스트

- 비로그인 앨범 접근 거부
- 다른 가족의 앨범과 사진 접근 거부
- 날짜당 10장 초과 거부
- 다른 날짜 사진을 대표 사진으로 지정하는 요청 거부
- 유효하지 않은 파일과 과대 파일 거부
- 완료 API 반복 호출의 멱등성
- 동일 SHA-256 파일의 원본 객체 재사용
- 필수 메타데이터 또는 썸네일 없는 사진의 `READY` 전환 거부
- RGW 성공·DB 실패 및 DB 성공·RGW 실패 복구
- 만료되거나 재사용된 초대 거부

### 22.2 브라우저 검증

- 가입 → 가족 생성 → 첫 업로드
- 가족 초대 → 가입 → 같은 앨범 확인
- 월 이동 → 날짜 상세 → 사진 상세
- 모바일 파일 선택과 업로드 진행률
- 일부 업로드 실패 후 재시도
- 키보드만으로 핵심 흐름 수행
- 360px, 768px, 1280px 화면 확인

### 22.3 저장소 통합 검증

- 업로드 객체와 DB 메타데이터 일치
- 동일 완료 요청 반복 시 중복 생성 방지
- 객체 업로드 성공 후 DB 실패 복구
- DB 레코드 생성 후 객체 누락 표시
- 재시도 후 동일 사진 정상 조회

## 23. 데모 시나리오

총 4~6분 기준:

1. 부모 계정으로 로그인한다.
2. 2026년 8월 캘린더에서 사진이 있는 날짜와 빈 날짜를 보여준다.
3. 빈 날짜에 사진 세 장을 올리고 업로드 진행률을 보여준다.
4. 동일 사진을 한 번 더 올려 중복 저장 방지 메시지를 보여준다.
5. 조부모 계정으로 전환해 같은 가족 앨범이 보이는지 확인한다.
6. 다른 가족 사진 URL에 접근할 수 없음을 보여준다.
7. 날짜당 10장 제한과 일부 업로드 실패 후 재시도를 보여준다.
8. 대표 사진을 바꾸고 캘린더에 즉시 반영되는지 확인한다.

## 24. 성공 기준

### 사용자 완성도

- 처음 방문한 사용자가 설명 없이 가입하고 첫 사진을 올릴 수 있다.
- 조부모 사용자가 모바일에서 최근 사진을 찾는 데 어려움이 없다.
- 빈 상태, 처리 중, 실패, 제한 초과 상황에 다음 행동이 보인다.
- 가족 외 사용자는 사진 URL을 알아도 접근할 수 없다.

### 애플리케이션 완성도

- 모든 핵심 요청이 Next.js, NestJS, PostgreSQL 및 Ceph 경계를 올바르게 통과한다.
- 동일 사진 반복 업로드가 실제 원본 중복 저장으로 이어지지 않는다.
- 캘린더와 목록에서 원본이 아닌 썸네일을 전송한다.
- 부분 실패가 사진 상태로 드러나며 재시도할 수 있다.

## 25. 주요 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| 8/18까지 범위 과다 | 핵심 흐름 미완성 | 사진 앨범·저장·관측 순으로 완성하고 부가 기능 제외 |
| RGW 브라우저 직접 업로드의 CORS/TLS 문제 | 업로드 차단 | Phase 0에서 presigned PUT 실험을 먼저 수행 |
| 이미지 처리 중 API 지연 | 업로드 체감 저하 | 파일 크기 제한, 썸네일 1종, 비동기 워커는 측정 후 판단 |
| 유사 사진 자동 삭제 | 사용자 데이터 손실 | 정확한 SHA-256만 자동 중복 제거 |
| DB와 객체 저장 불일치 | 깨진 사진·고아 객체 | 상태 머신, 멱등 완료 API, reconciliation 제공 |

## 26. 구현 전에 확정할 결정

다음 항목은 구현 전 팀이 한 번만 결정하면 된다.

1. 외부 도메인과 TLS 종료 위치
2. 브라우저가 접근할 RGW presigned URL의 외부 엔드포인트
3. 발표용 데모 계정과 2026년 8월 시드 사진
4. 초기 사진 최대 크기 20MB 승인 여부
5. 초대 링크 유효 기간
6. 원본 다운로드 제공 여부

권장 기본값:

- 초대 링크 7일
- 서명 URL 5분
- 세션 7일
- 사진 20MB
- 날짜당 10장
- 원본 다운로드는 OWNER와 MEMBER 모두 허용

## 27. 최종 방향

Family Frame의 완성도는 기능 수로 판단하지 않는다. 가족에게 로그인, 초대, 캘린더, 업로드, 다시 보기라는 짧은 흐름이 안정적으로 동작해야 한다.

따라서 우선순위는 다음과 같다.

```text
가족 사진을 안전하게 올리고 다시 본다
→ 실패와 장애에서도 상태를 잃지 않는다
→ 모바일과 데스크톱에서 편하게 사용한다
→ 그 이후에만 부가 기능을 추가한다
```

이 순서를 지키면 웹 서비스는 시스템 프로젝트의 부속 화면이 아니라 독립적으로 완성된 가족 앨범 제품이 된다.
